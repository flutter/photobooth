name: example
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tensorflow_models:
    path: ../

dev_dependencies:
  very_good_analysis: ^2.0.3

flutter:
  uses-material-design: true
