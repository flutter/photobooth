// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';
import '../lib/src/tensorflow_plugin.dart';

void main() {
  group('TensorflowWeb', () {
    test('can be instantiated', () {
      expect(TensorflowWeb(), isNotNull);
    });
  });
}
